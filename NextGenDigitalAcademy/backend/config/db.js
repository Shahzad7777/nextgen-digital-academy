// Database configuration placeholder.
// Currently using file-based storage (store.js).
// Replace with MongoDB/PostgreSQL connection when ready for production.

export const dbConfig = {
  type: 'file',
  description: 'File-based JSON storage for development. Swap with MongoDB for production.'
};
